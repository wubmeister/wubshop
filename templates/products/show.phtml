<h2>Product</h2>

<ul class="tabs">
    <li class="active"><a href="<?php $helper->routing->route([ "products" => $item->id ]); ?>">Product</a></li>
    <li><a href="<?php $helper->routing->route([ "products" => $item->id ], "edit"); ?>">Edit</a></li>
    <li><a href="<?php $helper->routing->route([ "products" => $item->id, "variants" ]); ?>">Variants</a></li>
</ul>

<table class="detail table">
    <tr>
        <td>Title</td>
        <td><?php echo $item->title; ?></td>
    </tr>
    <tr>
        <td>Type</td>
        <td><?php echo $item->product_type->name; ?></td>
    </tr>
    <tr>
        <td>Base price</td>
        <td><?php echo $item->base_price; ?></td>
    </tr>
    <tr>
        <td>SKU</td>
        <td><?php echo $item->sku; ?></td>
    </tr>
    <tr>
        <td>GTIN</td>
        <td><?php echo $item->gtin; ?></td>
    </tr>
</table>

<p>
    <a href="<?php $helper->routing->route([ "products" => $item->id ], "edit"); ?>">Edit</a>
    &middot;
    <a href="<?php $helper->routing->route([ "products" => $item->id ], "delete"); ?>">Delete</a>
</p>
<p><a href="<?php $helper->routing->route([ "products" ]); ?>">Back to index</a></p>
